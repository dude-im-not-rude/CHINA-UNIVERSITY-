import { neon } from '@neondatabase/serverless';
import { execFile } from 'node:child_process';
import { promisify } from 'node:util';
import { mkdtemp, writeFile, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
const execFileAsync=promisify(execFile);
const db=process.env.DATABASE_URL?neon(process.env.DATABASE_URL):null;if(!db)throw new Error('DATABASE_URL is required for official program import.');
const USER_AGENT='ChinaUniTracker-OfficialProgramImporter/3.3',TIMEOUT_MS=30000,RETRIES=3,MAX_PAGES_PER_UNIVERSITY=120,ACCEPTED_YEARS=['2026','2027','2028'];
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function normalizeDocumentUrl(url){try{const u=new URL(url);if(u.hostname==='drive.google.com'||u.hostname==='drive.usercontent.google.com'){const id=u.pathname.match(/\/file\/d\/([^/]+)/)?.[1]||u.searchParams.get('id');if(id)return`https://drive.usercontent.google.com/download?id=${encodeURIComponent(id)}&export=download&confirm=t`;}if(u.hostname==='docs.google.com'){const m=u.pathname.match(/\/(document|spreadsheets)\/d\/([^/]+)/);if(m)return m[1]==='document'?`https://docs.google.com/document/d/${m[2]}/export?format=txt`:`https://docs.google.com/spreadsheets/d/${m[2]}/export?format=csv`}}catch{}return url}
function isDocumentHost(url){try{const h=new URL(url).hostname;return h==='drive.google.com'||h==='docs.google.com'||h==='drive.usercontent.google.com'||h.endsWith('.googleusercontent.com')}catch{return false}}
async function fetchText(url){const target=normalizeDocumentUrl(url);let last;for(let attempt=1;attempt<=RETRIES;attempt++){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),TIMEOUT_MS);try{const response=await fetch(target,{headers:{'user-agent':USER_AGENT,accept:'text/html,application/xhtml+xml,application/pdf,text/plain,text/csv,*/*;q=0.7','accept-language':'en-US,en;q=0.8'},redirect:'follow',signal:controller.signal});if(!response.ok)throw new Error(`${response.status} ${response.statusText}`);const type=(response.headers.get('content-type')||'').toLowerCase(),finalUrl=response.url,isPdf=type.includes('application/pdf')||/\.pdf(?:$|[?#])/i.test(finalUrl);if(isPdf){const bytes=Buffer.from(await response.arrayBuffer()),dir=await mkdtemp(join(tmpdir(),'chinauni-pdf-')),file=join(dir,'source.pdf');try{await writeFile(file,bytes);const{stdout}=await execFileAsync('pdftotext',['-layout',file,'-'],{maxBuffer:12*1024*1024});return{url:finalUrl,html:stdout,isPdf:true}}finally{await rm(dir,{recursive:true,force:true})}}return{url:finalUrl,html:await response.text(),isPdf:false}}catch(error){last=error;if(attempt<RETRIES)await sleep(attempt*1000)}finally{clearTimeout(timer)}}throw last}
function clean(html){return html.replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<noscript[\s\S]*?<\/noscript>/gi,' ').replace(/<[^>]+>/g,' ').replace(/&nbsp;/gi,' ').replace(/&amp;/gi,'&').replace(/&quot;/gi,'"').replace(/&#39;/gi,"'").replace(/\s+/g,' ').trim()}
function links(html,base){const out=new Map(),re=/<a\b[^>]*href\s*=\s*["']([^"']+)["'][^>]*>([\s\S]*?)<\/a>/gi;let m;while((m=re.exec(html))){try{const u=new URL(m[1],base);if(/^https?:$/i.test(u.protocol))out.set(u.href,clean(m[2]).slice(0,180))}catch{}}return[...out.entries()]}
function sameSite(a,root){try{const x=new URL(a),r=new URL(root);return x.hostname===r.hostname||x.hostname.endsWith(`.${r.hostname}`)}catch{return false}}
function title(html,url){const h1=html.match(/<h1[^>]*>([\s\S]*?)<\/h1>/i)?.[1],h2=html.match(/<h2[^>]*>([\s\S]*?)<\/h2>/i)?.[1],t=html.match(/<title[^>]*>([\s\S]*?)<\/title>/i)?.[1];return clean(h1||h2||t||new URL(url).pathname.split('/').filter(Boolean).pop()||'Program').replace(/\s*[|–—-]\s*(official.*|home.*)$/i,'').slice(0,180)}
function degree(text,url=''){const h=`${url} ${text}`.toLowerCase();if(/\b(phd|doctoral|doctorate)\b/.test(h))return'phd';if(/\b(master|msc|mba|graduate)\b/.test(h))return'master';if(/\b(bachelor|bsc|ba|bba|undergraduate)\b/.test(h))return'bachelor';if(/\b(professional training|professional program|professional course|executive program)\b/.test(h))return'professional';if(/\b(language program|language course|chinese language|mandarin)\b/.test(h))return'language';if(/\b(summer camp|summer school|winter camp|winter school)\b/.test(h))return'summer';if(/\b(visiting student|visiting program|exchange program|study abroad|short[- ]term)\b/.test(h))return'non-degree';if(/\b(training|certificate|continuing education|non[- ]degree)\b/.test(h))return'non-degree';return null}
function language(text){if(/\b(bilingual|english and chinese|chinese and english)\b/i.test(text))return'Bilingual';if(/\b(english[- ]taught|taught in english|language of instruction[^.]{0,40}english|teaching language[^.]{0,40}english)\b/i.test(text))return'English';if(/\b(chinese[- ]taught|taught in chinese|language of instruction[^.]{0,40}chinese|teaching language[^.]{0,40}chinese)\b/i.test(text))return'Chinese';return'Other'}
function duration(text){const m=text.match(/(?:duration|study period)[^\d]{0,30}(\d+(?:\.\d+)?)\s*(?:years?|yrs?)/i);return m?Number(m[1]):null}
function tuition(text){const m=text.match(/(?:tuition|tuition fee|tuition fees)[^\d]{0,40}(?:rmb|cny|yuan)?\s*([\d,]+(?:\.\d+)?)/i);return m?Number(m[1].replace(/,/g,'')):null}
function deadline(text){const m=text.match(/(?:application deadline|deadline|apply by)[^\n|:]{0,80}((?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)[a-z]*\s+\d{1,2}(?:,\s*|\s+)\d{4}|\d{4}[/-]\d{1,2}[/-]\d{1,2})/i);if(!m)return null;const d=new Date(m[1].replace(/-/g,'/'));return Number.isNaN(d.getTime())?null:d.toISOString().slice(0,10)}
function year(text){return ACCEPTED_YEARS.find(y=>new RegExp(`\\b${y}\\b`).test(text))||null}
const BAD_NAME=/^(home|homepage|admissions?|application( process| procedures?| requirements?| documents?)?|admission( process| procedures?| requirements?)?|academic calendar|international students?|undergraduate( programs?)?|graduate( programs?)?|programs?|courses?|academics?|study|news|notice|school|college|faculty|application|scholarship(s)?( programs?)?|exchange( programs?)?|department(s)?|schools? (&|and) departments|fees?|tuition|accommodation|how to apply|contact( us)?|about( us)?|basic information|university information|semester arrangement.*)$/i;
const PROGRAM_PATH=/(^|[/_-])(programs?|programmes?|courses?|majors?|degrees?|bachelor|master|mba|undergraduate|graduate|phd|doctoral|professional|language|summer|winter|training|visiting|exchange|short[-_ ]term)([/_.?&#=-]|$)/i;
const ADMIN_PATH=/(application|admission|requirement|procedure|document|calendar|scholarship|exchange|department|faculty|school|contact|about|fees|tuition|accommodation|how[-_ ]to[-_ ]apply)/i;
function isRealProgramPage(url,anchor,name,text,isPdf=false){const path=new URL(url).pathname,hay=`${url} ${anchor}`,d=degree(text,url);if(!isPdf&&!name)return false;if(!d)return false;if(isPdf)return/\b(program|major|degree|bachelor|master|phd|professional|language|summer|training|visiting|exchange|tuition|admission)\b/i.test(text);if(BAD_NAME.test(name.trim())||!PROGRAM_PATH.test(hay))return false;if(ADMIN_PATH.test(path)&&!PROGRAM_PATH.test(path)&&!PROGRAM_PATH.test(anchor))return false;return true}
async function upsert(universityId,universityName,record){if(!record.name||record.name.length<3||BAD_NAME.test(record.name)||!record.degree)return false;const rows=await db`SELECT id FROM programs WHERE university_id=${universityId} AND lower(program_name)=lower(${record.name}) AND lower(degree_level)=lower(${record.degree}) LIMIT 1`;let id=rows[0]?.id;if(id)await db`UPDATE programs SET english_taught=COALESCE(english_taught,${record.language==='English'}),duration_years=COALESCE(duration_years,${record.duration}),tuition_fee=COALESCE(tuition_fee,${record.tuition}),tuition_currency=COALESCE(tuition_currency,${record.tuition!=null?'RMB':null}),official_program_url=COALESCE(official_program_url,${record.url}),is_active=true,updated_at=now() WHERE id=${id}`;else{const r=await db`INSERT INTO programs (university_id,program_name,degree_level,language,english_taught,duration_years,tuition_fee,tuition_currency,official_program_url,is_active) VALUES (${universityId},${record.name},${record.degree},${record.language},${record.language==='English'},${record.duration},${record.tuition},${record.tuition!=null?'RMB':null},${record.url},true) RETURNING id`;id=r[0].id}if(record.year){const start=`${record.year}-09-01`,status=record.deadline&&new Date(record.deadline)<new Date()?'closed':'upcoming',existing=await db`SELECT id FROM intakes WHERE program_id=${id} AND semester_start_date=${start} LIMIT 1`;if(existing[0])await db`UPDATE intakes SET application_deadline=COALESCE(application_deadline,${record.deadline}),application_status=${status},updated_at=now() WHERE id=${existing[0].id}`;else await db`INSERT INTO intakes (program_id,intake_name,application_deadline,semester_start_date,application_status,notes) VALUES (${id},'September',${record.deadline},${start},${status},'Discovered from an official university webpage or linked university document.')`}const source=await db`SELECT id FROM sources WHERE program_id=${id} AND source_url=${record.url} LIMIT 1`;if(source[0])await db`UPDATE sources SET last_checked_at=now(),verification_status='verified',notes='Program discovered from an official university webpage or linked university document.' WHERE id=${source[0].id}`;else await db`INSERT INTO sources (program_id,source_name,source_url,source_type,is_official,last_checked_at,verification_status,notes) VALUES (${id},${universityName},${record.url},'official',true,now(),'verified','Program discovered from an official university webpage or linked university document; used to enrich missing fields without overriding trusted reference values.')`;return true}
const universities=await db`SELECT id,name_english,official_website FROM universities WHERE country='China' AND status='active' AND official_website IS NOT NULL AND trim(official_website)<>'' ORDER BY id`;console.log(`[OFFICIAL-ALL] ${universities.length} universities queued; no fixed university-count limit.`);let total=0;
for(const u of universities){const queue=[[u.official_website,'']],seen=new Set(),candidates=new Map();let pages=0;while(queue.length&&pages<MAX_PAGES_PER_UNIVERSITY){const[url,anchor]=queue.shift();if(seen.has(url))continue;seen.add(url);try{const p=await fetchText(url);pages++;const txt=p.isPdf?p.html:clean(p.html),name=p.isPdf?(url.split('/').pop()||'University document'):title(p.html,p.url);if(isRealProgramPage(p.url,anchor,name,txt,p.isPdf))candidates.set(p.url,{text:txt,degree:degree(txt,p.url),name});if(!p.isPdf)for(const[link,a]of links(p.html,p.url)){const path=new URL(link).pathname,doc=isDocumentHost(link)||/\.pdf(?:$|[?#])/i.test(link);if((sameSite(link,u.official_website)||doc)&&!seen.has(link)&&(!ADMIN_PATH.test(path)||PROGRAM_PATH.test(path)||PROGRAM_PATH.test(a)||doc))queue.push([link,a])}await sleep(100)}catch(e){console.warn(`[OFFICIAL-ALL] ${u.name_english}: ${url} :: ${e.message}`)}}let imported=0;for(const[url,c]of candidates){const parsedName=c.name.replace(/\.(pdf|txt|csv)$/i,'').replace(/[_-]+/g,' ').replace(/\s+/g,' ').trim(),record={name:parsedName,degree:c.degree,language:language(c.text),duration:duration(c.text),tuition:tuition(c.text),year:year(c.text),deadline:deadline(c.text),url};try{if(await upsert(u.id,u.name_english,record))imported++}catch(e){console.warn(`[OFFICIAL-ALL] ${u.name_english}: import failed ${record.name} :: ${e.message}`)}}total+=imported;console.log(`[OFFICIAL-ALL] ${u.name_english}: ${pages} pages/documents, ${candidates.size} verified candidates, ${imported} imported/updated`)}console.log(`[OFFICIAL-ALL] complete: ${universities.length} universities scanned; ${total} program records imported/updated.`);